// Package verifiers ships ready-made implementations of
// clients/webhooks.Verifier. Each verifier targets either a
// configurable HMAC scheme (GenericHMAC — for most partners) or a
// concrete provider with hard-coded conventions (GitHub).
package verifiers
